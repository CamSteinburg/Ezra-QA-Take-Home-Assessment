# Optional — defaults match `playwright.config.ts` (staging URL) if omitted.
# BASE_URL=https://myezra-staging.ezra.com

# Optional (only if BASE_URL is omitted — prefer setting BASE_URL above)
# TEST_ENV=staging          # local | dev | qa | staging (default: staging)

# ----- Scheduling (optional) -----
# Comma-separated time labels for pickSlot (see fixtures/bookingDefaults.ts). If unset, a default
# half-hour bank from 7:00 AM–9:00 PM is used.
# APPT1_TIMES=7:00 AM,7:30 AM,8:00 AM
